services:
  dotnet:
    build:
      context: ../..
      dockerfile: scripts/docker/Dockerfile
    working_dir: /workspace
    environment:
      - NUGET_API_KEY
      - COMETBFT_RPC_URL
      - COMETBFT_WS_URL
      - COMETBFT_GRPC_URL
